# frozen_string_literal: true

class AddColumn<%= options[:column].classify %>To<%= params.raw.model.name.classify.pluralize %> < ActiveRecord::Migration[<%= options[:rails_version] %>]
  def change
    add_column <%= params.raw.model.name %>, :<%= options[:column] %>, :<%= options[:type] %>
  end
end
