<%- params = Templaty::Params.new(options) -%>
pt-BR:
  <%= params.raw.namespace %>:
    <%= params.model_plural %>:
      show:
        not_found: <%= params.raw.model.i18n['pt-BR'].one %> inexistente.
