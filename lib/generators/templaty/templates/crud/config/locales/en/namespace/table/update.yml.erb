en:
  <%= options[:namespace] %>:
    <%= options[:table] %>:
      update:
        not_found: <%= options[:table].capitalize %> not found.
        success: <%= options[:table].capitalize %> updated successfully.
