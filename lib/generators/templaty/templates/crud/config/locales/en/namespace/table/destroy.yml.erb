en:
  <%= options[:namespace] %>:
    <%= options[:table] %>:
      destroy:
        not_found: <%= options[:table].capitalize %> not found.
        success: <%= options[:table].capitalize %> successfully removed.
